<template>
	<h1>At 规则</h1>

	<p>At 规则是一个 CSS 语句，用来指示 CSS 如何运行。以 at 符号‘@’开头，后跟一个标识符，并包括直到下一个分号‘;’的所有内容，或下一个 CSS 块，以先到者为准。</p>


	<p>规则</p>


	<pre>
		<code v-code:css>
/* 一般结构 */
@identifier (RULE);

/* 示例：通知浏览器使用 UTF-8 字符集 */
@charset "utf-8";
		</code>
	</pre>

	<p>at 规则，由它们的标示符指定，每种规则都有不同的语法。</p>
</template>