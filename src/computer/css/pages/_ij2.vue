<template>
	<h1>&lt;length&gt;</h1>


	<p>长度 ‘&lt;length&gt;’ 是用于表示距离尺寸的 CSS 数据类型。许多 CSS 属性会用到长度，比如 ‘width’、‘margin’、‘padding’、‘font-size’、‘border-width’ 和
		‘text-shadow’。</p>



	<section class="note">
		<strong>备注：</strong>
		虽然 ‘&lt;percentage&gt;’ 值也是 CSS 维度，并且可以在接受 ‘&lt;length&gt;’ 值的某些相同属性中使用，但它们本身不是 ‘&lt;length&gt;’ 值。
	</section>

	<h3>语法</h3>
	<p>‘&lt;length&gt;’ 数据类型由一个 ‘&lt;number&gt;’ 和一个长度单位构成。与所有 CSS 维度一样，单位的字面值与数字之间没有空格。数字为 0 时，长度单位是可选的。</p>


	<blockquote>有些属性支持使用负数的长度值，而有些不支持。</blockquote>
</template>