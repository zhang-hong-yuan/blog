<template>
	<h1>Screen</h1>

	<p><strong>window.screen</strong>对象在编写时可以不使用 window 这个前缀。</p>
	<p>screen.availWidth 属性返回访问者屏幕的宽度，screen.availHeight 属性返回访问者屏幕的高度，以像素计，减去界面特性，比如窗口任务栏。</p>
	<ul>
		<li><code>screen.width</code> 屏幕宽度，以像素为单位</li>
		<li><code>screen.height</code>屏幕高度，以像素为单位</li>
		<li><code>screen.availWidth</code> 可用的屏幕宽度</li>
		<li><code>screen.availHeight</code> 可用的屏幕高度</li>

	</ul>
</template>