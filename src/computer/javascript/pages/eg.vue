<template>
	<h1>string.toLowerCase()</h1>
	<p>【String】 的 【toLowerCase()】 方法《将该字符串转换为小写形式》。返回《一个新的字符串》。</p>



	<pre v-code:js>string.《toLowerCase()》</pre>

	<p>例如：</p>
	<pre v-code:js>console.log("ALPHABET".toLowerCase()); // 'alphabet'</pre>


	<hr>

	<h1>string.toUpperCase()</h1>
	<p>【String】 的 【toUpperCase()】 方法《将该字符串转换为大写形式》。返回《一个新的字符串》。</p>

	<p>由于 JavaScript 中的字符串是不可变的，因此此方法《不会::会不会》影响字符串本身的值。</p>

	<pre v-code:js>string.toUpperCase()</pre>

	<p>示例：</p>

	<pre v-code:js>console.log("alphabet".《toUpperCase()》); // 'ALPHABET'</pre>
</template>
