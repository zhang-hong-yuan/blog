<template>

	<h1>id</h1>
	<p>id 属性规定 HTML 元素的唯一的 id。id 属性可用作链接锚 (link anchor)，通过 JavaScript 或通过 CSS 为带有指定 id 的元素改变或添加样式。</p>

	<table>
		<tr>
			<th>属性值</th>
			<th>描述</th>
		</tr>
		<tr>
			<td><strong>id</strong></td>
			<td>
				<p>规定元素的唯一 id。命名规则：</p>
				<ul>
					<li>必须以字母 A-Z 或 a-z 开头</li>
					<li>其后的字符：字母(A-Za-z)、数字(0-9)、连字符(&quot;-&quot;)、下划线(&quot;_&quot;)</li>
					<li>值对大小写敏感</li>
				</ul>
			</td>
		</tr>
	</table>

</template>